#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.9.1)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

from mondix_change_interface import TeiidChangeService
from mondix_change_interface.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  int performBinaryInsert(string relationName, string column1, string column2)'
  print '  int performBinaryDelete(string relationName, string column1, string column2)'
  print '  int performBinaryUpdate(string relationName, string old_column1, string old_column2, string new_column1, string new_column2)'
  print '  int performUnaryInsert(string relationName, string column1)'
  print '  int performUnaryDelete(string relationName, string column1)'
  print '  int performUnaryUpdate(string relationName, string column1)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = TeiidChangeService.Client(protocol)
transport.open()

if cmd == 'performBinaryInsert':
  if len(args) != 3:
    print 'performBinaryInsert requires 3 args'
    sys.exit(1)
  pp.pprint(client.performBinaryInsert(args[0],args[1],args[2],))

elif cmd == 'performBinaryDelete':
  if len(args) != 3:
    print 'performBinaryDelete requires 3 args'
    sys.exit(1)
  pp.pprint(client.performBinaryDelete(args[0],args[1],args[2],))

elif cmd == 'performBinaryUpdate':
  if len(args) != 5:
    print 'performBinaryUpdate requires 5 args'
    sys.exit(1)
  pp.pprint(client.performBinaryUpdate(args[0],args[1],args[2],args[3],args[4],))

elif cmd == 'performUnaryInsert':
  if len(args) != 2:
    print 'performUnaryInsert requires 2 args'
    sys.exit(1)
  pp.pprint(client.performUnaryInsert(args[0],args[1],))

elif cmd == 'performUnaryDelete':
  if len(args) != 2:
    print 'performUnaryDelete requires 2 args'
    sys.exit(1)
  pp.pprint(client.performUnaryDelete(args[0],args[1],))

elif cmd == 'performUnaryUpdate':
  if len(args) != 2:
    print 'performUnaryUpdate requires 2 args'
    sys.exit(1)
  pp.pprint(client.performUnaryUpdate(args[0],args[1],))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
